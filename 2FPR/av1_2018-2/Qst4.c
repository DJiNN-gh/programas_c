/* Questão 04 [2,5 pontos]:
Determinada zona eleitoral possui N eleitores inscritos que poderão votar, uma única 
vez, em um dos C candidatos para o único cargo a ser definido. Pede-se:

a. A declaração do vetor de structs, assim como de todos os tipos de dados necessários, que represente os resultados da votação;
b. Uma função que leia o voto de cada eleitor, considerando que o processo terminará 
quando os N eleitores votarem ou quando o número -1 for digitado como voto;
c. Uma função que exiba:

	i. O total de votos para cada candidato, assim com a quantidade de votos brancos
(código 0) e de nulos (código sem candidato associado);
	ii. A quantidade de abstenções (eleitores que não votaram);
	iii. O nome do candidato vencedor (caso tenha mais de 50% dos votos válidos) ou dos que irão para o segudo turno (os dois mais votados).

Nota: o número do candidato deve conter dois algarismos apenas. */
