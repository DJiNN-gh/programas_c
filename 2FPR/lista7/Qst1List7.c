/* Questão 01:
Desenvolver uma função que, dado o nome de um arquivo texto contendo números 
(um por linha), determine se estes encontram-se ordenados crescentemente. */

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <limits.h>

void fatal (char *msg);

int main (int argc, char *argv[]){

	FILE *f;

	



return 0;}

void fatal (char *msg){

	fprintf(stderr, "Erro: %s\n", msg);
	exit(1);}
